/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/ChipHappens',
  assetPrefix: '/ChipHappens/',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
